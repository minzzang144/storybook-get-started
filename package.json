{
    "name": "root",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "clean": "rm -rf node_modules && rm -f yarn.lock && lerna clean -y && yarn cache clean"
    },
    "devDependencies": {
        "lerna": "^5.5.4"
    },
    "version": "0.1.0",
    "dependencies": {
        "babel-loader": "8.1.0",
        "webpack": "4.44.2"
    }
}
